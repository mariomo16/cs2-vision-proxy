info:
  name: /players/{id}
  type: http
  seq: 1

http:
  method: GET
  url: https://csrep.gg/api/players/{{id}}
  headers:
    - name: X-API-Key
      value: "{{SECRET_TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
