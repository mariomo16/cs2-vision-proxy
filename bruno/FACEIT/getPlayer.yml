info:
  name: getPlayer
  type: http
  seq: 2

http:
  method: GET
  url: https://open.faceit.com/data/v4/players/{{player_id}}
  headers:
    - name: Authorization
      value: Bearer {{API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
