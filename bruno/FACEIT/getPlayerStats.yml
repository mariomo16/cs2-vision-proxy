info:
  name: getPlayerStats
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/faceit/players/{{player_id}}/stats/cs2
  headers:
    - name: Authorization
      value: Bearer {{API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
